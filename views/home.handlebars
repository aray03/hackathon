

    {{#if renderResult}}
        
        <p>{{result.message}}</p>
        {{#if result.confidence}}
            <p>Confidence: {{result.confidence}}%</p>
        {{/if}}
    {{else}}
        <p>No result to display yet. Please upload an image.</p>
    {{/if}}

